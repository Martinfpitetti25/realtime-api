websocket-client>=1.7.0
python-dotenv>=1.0.0
pyaudio>=0.2.14
numpy>=1.24.0
scipy>=1.4.1
requests>=2.31.0

# Wake Word Detection (opcional)
pvporcupine>=3.0.0

# Computer Vision (para detección de objetos)
# opencv-contrib-python — NO instalar desde pip. Usar el paquete del sistema:
#   sudo apt install python3-opencv
#   El venv debe crearse con: python3 -m venv --system-site-packages .venv
#   Después de cualquier `pip install`, ejecutar: pip uninstall -y opencv-python opencv-contrib-python
ultralytics>=8.0.196
# mediapipe>=0.10.14  # Opcional - solo para face tracking (requiere Python <3.12)

# Eye Tracking / Servo Control (frankenstein integration)
adafruit-circuitpython-servokit>=1.3.21
rpi-lgpio>=0.4  # Solo en Raspberry Pi
